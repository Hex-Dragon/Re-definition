using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainM : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        TextPic.ResetIconList();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
